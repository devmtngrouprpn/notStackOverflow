const initialState = {};

// --- Action Types --- //

// --- Reducer --- //

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return { state };
  }
}

// --- Action Creators --- //
