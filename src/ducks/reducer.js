let initialState = {
    home: {

    },
    question: {

    },
    tags: {

    },
    user: {

    },
};

// --- Action Types --- //

// --- Reducer Function --- //

export default function reducer(state = initialState, action) {
    switch (action.type) {
        default:
            return { ...state }
    }
}

// --- Action Creators --- //